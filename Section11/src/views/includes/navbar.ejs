<nav class='navbar navbar-expand-lg navbar-dark bg-dark'>
    <div class='container'>
        <a class='navbar-brand' href='/'>Agenda</a>

        <button class='navbar-toggler' type='button' data-bs-toggle='collapse' data-bs-target='#navbarSupportedContent'
            aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation'>
            <span class='navbar-toggler-icon'></span>
        </button>

        <div class='collapse navbar-collapse' id='navbarSupportedContent'>
            <% if(user) { %>
                <ul class='navbar-nav me-auto mb-2 mb-lg-0'>
                    <li class='nav-item'>
                        <a class='nav-link' href='/contact'>Create Contact</a>
                    </li>
                </ul>
                
                <div class='text-end'>
                    <button type='button' class='btn btn-primary'><a href='/account'>My Account</a></button>
                    <button type='button' class='btn btn-outline-light me-2'><a href='/login/logout'>Logout</a></button>
                </div>
            <% } else { %>
                <div class='ms-auto text-end'>
                    <button type='button' class='btn btn-outline-light me-2'><a href='/login'>Login</a></button>
                    <button type='button' class='btn btn-warning'><a href='/register'>Sign-up</a></button>
                </div>
            <% } %>
        </div>
    </div>
</nav>